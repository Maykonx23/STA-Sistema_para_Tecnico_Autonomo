/* import { render, screen } from "@testing-library/react";
import "reflect-metadata";
import { Button } from "src/Components/Buttons";

describe("Verificar se a Mensagem Bem Vindo esta sendo Renderizada", () => {
    test("Teste", () => {
        render(Button);
    });
});
 */
